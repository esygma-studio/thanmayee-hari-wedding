/* ================================================================
   WEDDING WEBSITE — API KEYS
   ================================================================ */

// ── Firebase Realtime Database (guest counter) ───────────────────
// 1. Go to https://console.firebase.google.com
// 2. Create project → Build → Realtime Database → Start in test mode
// 3. Paste the database URL below (https://xxx-rtdb.firebaseio.com)

const FIREBASE_DB_URL = 'YOUR_FIREBASE_DB_URL';

// ── Resend (email) ───────────────────────────────────────────────
// API key goes in Vercel → Project Settings → Environment Variables
// Variable name: RESEND_API_KEY   (never put the key here in the browser)
